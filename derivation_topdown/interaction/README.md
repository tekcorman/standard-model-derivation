# interaction — Interval 3 of the staged program

## THE WALL (same binding rule)

**PURE MATHEMATICS. No physics — no SM, no Cl(6), no specific particles/couplings — without specific
user permission.** Comparison to the broader project is DEFERRED and permission-gated; the physics
*reading* stays in chat only.

## What this is

Interval 3: add an **interaction** (a four-fermion / quartic term) to the second-quantized srs field.
The push (time_bridge/t04) proved the *free* field + the III_1 structure **cannot** fix the scale — the
matter gap m and the KMS β are free inputs. An interaction is the **only** remaining route to a scale
from *within* the wall: a self-consistent gap equation can dynamically generate one.

## The only question (pure math)

> Does an attractive four-fermion interaction produce a self-consistent gap m, and is the **dimensionful**
> scale m bootstrapped from a **dimensionless** coupling + the geometric bandwidth (dimensional
> transmutation)? If so, the wall generates its own scale (trading a dimensionful input for a dimensionless one).

## Discipline

Compute, don't assert; verify; forced vs free honestly; no rounding; flag overclaims in real time. Builds
on the verified `../dirac_srs_mdl/` (the band DOS) and the two bridges. Standard many-body / NJL–BCS
mean-field math; no physics specifics.
