# time_bridge — Interval 2 of the staged program

## THE WALL (same binding rule)

**PURE MATHEMATICS. No physics — no Standard Model, no Cl(6), no "modular time = physical/cosmic time"
claim, no particles/cosmology — without specific user permission.** Comparison to the broader project is
DEFERRED and permission-gated. The physics *reading* stays in chat only.

## What this is

Interval 2: the **time bridge**. The bare object (`../dirac_srs_mdl/`) is **static** — its natural trace
is tracial, so (Tomita–Takesaki) its modular flow is trivial: no intrinsic time. This interval adds
exactly **one** ingredient — a (non-tracial) state — and derives what time-structure flows from it.

## The only question (pure math)

> Given the srs spectral-triple algebra and a non-tracial state, what canonical flow (a 1-parameter
> automorphism group = "time") does Tomita–Takesaki give, what generates it, and what **scale** does it
> carry?

Assume no answer. Report forced vs. free honestly.

## Discipline

Compute, don't assert; verify load-bearing claims; no rounding; flag overclaims in real time. Builds on
the verified `../dirac_srs_mdl/`. The one **free strength** left by the matter bridge (Interval 1) is the
seam — a scale enters here. **Fixing the matter strength FROM this scale is the later, permission-gated
cross-pollination — NOT done in this interval.**
