# time_bridge — findings (Interval 2)

Pure math; **no physics** (see README). Builds on the verified `../dirac_srs_mdl/`.

## t01 — the modular (Tomita–Takesaki) flow: time from a non-tracial state
- The bare object's **tracial** state ⇒ **trivial** modular flow (σ_t = id): the object is **STATIC**
  (rigorous confirmation of the bare-object finding).
- A **non-tracial** state ⇒ a canonical modular flow `σ_t(x) = ρ^{it} x ρ^{−it}`, generated by the
  **modular Hamiltonian K = −log ρ**. For the geometric state ρ ∝ e^(−βH) (H = the Bloch Dirac/Laplacian),
  this is the **unitary evolution by H**; β is the (input) scale (slope β = 1 verified).
- **KMS at β = 1**: the state is the **equilibrium** state of its own modular flow — time is
  self-consistent (the state is stationary under the very flow it generates). Verified.
- **FINDING:** time **emerges from the (non-tracial) state**, generated by the geometric Hamiltonian;
  the bare object alone is static. (Parallels m01: the bare object lacks the feature; one added
  ingredient supplies it.)

## t02 — the algebra TYPE: state-dependent time, not intrinsic (THEOREM)
The srs band algebra L^∞(T³) ⊗ M₄ is **TYPE I**: it has a faithful normal **trace** (⇒ NOT type III) and
**minimal projections** (⇒ type I, not II). Structural reason: srs is an **amenable** crystal (ℤ³-periodic;
Bloch ⇒ direct integral of matrix factors), so it is type I **despite** the positive NB-walk entropy log 2
— *amenability, not entropy, fixes the vN type* (the geodesic flow's entropy is a separate **classical**
dynamics needing an initial condition, not an intrinsic modular time). Consequence (Connes): type I ⇒
every modular flow is **inner** ⇒ the canonical ℝ → Out(M) is trivial ⇒ **NO intrinsic (state-independent)
time**. The t01 flow is **state-dependent**, and its scale is correspondingly **free**.

## INTERVAL 2 — LANDED (time bridge)
The bare object is static (tracial). A non-tracial state gives modular time (the **mechanism**), generated
by the geometric Hamiltonian K = −log ρ; but the algebra is **type I**, so that time is **state-dependent**
(inner), **not intrinsic**, and its **scale is free** (a state-choice).
- **FORCED:** the modular-flow *mechanism* (time from a non-tracial state, generated by the geometric H).
- **FREE:** the state — hence the time and its scale.

**This mirrors the matter bridge exactly:** the geometry forces the **structure/mechanism** (Interval 1:
the chiral Dirac Cl(4); Interval 2: the modular flow) but leaves the **scales/choices** free (Interval 1:
gauge algebra + gap strength; Interval 2: the state + time-scale).

## t03 — the type-III route: intrinsic time, ACHIEVED at the second-quantized level
Second-quantize the srs one-particle Dirac space (the CAR algebra) + a quasi-free KMS state ω_β
(Fermi–Dirac two-point function). The GNS factor is **Araki–Woods**, and the srs band spectrum is
**absolutely continuous** (dispersive bands, ranges 1.72–2.31, no flat band) ⇒ asymptotic ratio set ℝ_+
⇒ **TYPE III_1** (verified: the modular / energy-difference spectrum spans [0, 5.93] **densely**, max gap
0.034 → 0 — not a lattice (log λ)·ℤ). For type III_1 the modular flow is **outer** (Connes-canonical,
state-independent) = **intrinsic time, generated by the one-particle Dirac D** (modular Hamiltonian =
β·dΓ(D); KMS identity e^(−βh)(1−ρ)=ρ verified).
- **⇒ intrinsic time IS realized** — at the second-quantized (field) level; the time the type-I band
  algebra (t02) lacked. **New ingredient:** second quantization (a field). **Remaining parameter:** the
  KMS β (a normalization).
- *Verification catch (4th this arc): an initial energy-difference subsample took only the band bottom
  (span [0, 0.96]); corrected to span the full band ([0, 5.93]) before recording.*

So the time bridge has both levels: **first-quantized (band) ⇒ type I ⇒ state-dependent time** (t02);
**second-quantized (field) + KMS ⇒ type III_1 ⇒ intrinsic time generated by D** (t03).

## t04 — pushing type III: scale-free, β not fixed, the scales are free inputs
Decisive (walled): **(1)** the factor is III_1 for **all β > 0** (the ratio set is ℝ_+ regardless) ⇒ the
*type does not fix β*. **(2)** III_1 is the **scale-free** type III — dense modular spectrum ⇒ Connes
**T(M) = {0}, no period** — unlike III_λ (a *lattice* ratio set ⇒ period 2π/|log λ| = an intrinsic scale).
srs (continuous bands, max gap 0.034 → 0) ⇒ scale-free; a hypothetical discrete spectrum (gap 0.5) would
be III_λ with a scale. **(3)** a matter gap m **keeps it III_1** (gapped bands are still continuous; the
gap is a *frequency* in D, not a new von Neumann scale), and a *free* field has **no self-consistent gap
equation** (m is bilinear, not self-generated).
- **⇒ the two scales (matter gap m, KMS β) are GENUINE FREE INPUTS.** The III_1 structure and free-field
  self-consistency *cannot* fix them. To fix them needs **either** a new walled ingredient (an
  *interaction* ⇒ a gap equation / self-consistency) **or** an external scale (the deferred cross-pollination).

**The wall's limit, precisely located:** the walled {D, srs, MDL} program **forces** all the
structures and the intrinsic flows (bare object; matter Cl(4) + gap-mechanism; time = the III_1 modular
flow generated by D), and leaves **exactly two free scales** (m, β) — and the time bridge, being
scale-free III_1, *provably cannot* supply the scale itself.

## t07 — the geodesic flow's forced RATES (the dynamics' dimensionless content)
Develops the non-backtracking (geodesic) flow B as a full hyperbolic dynamical system. Its entire rate
structure is **forced by k=3** through Ihara–Bass h²−λh+(k−1)=0; all quantities are **dimensionless**
(per-step / per-cell), confirming the III_1 scale-freeness — the dynamics forces RATIOS, never a scale.
- **Topological = KS entropy** h_top = log(k−1) = **log 2** (verified via (1/m)log Tr Bᵐ → log 2).
- **Correlation decay = the Ramanujan ratio 1/√(k−1) = 1/√2 per step** — DERIVED (not asserted) by
  iterating B/ρ on a Ramanujan-shell observable (fitted slope −0.34804 vs −log√2 = −0.34657). This is
  Alon–Boppana-saturated (optimal mixing). The slowest surviving mode (the |h|=1 "tree" ring) decays by
  the slower 1/(k−1)=1/2 per step (asymptotic rate). Mixing time 2/log 2 = 2.8854; additive gap 2−√2.
- **Heat flow (D²=L) diffusion tensor is ANISOTROPIC** — a genuine surprise given the net's strong
  geometric isotropy. The lowest (acoustic) band is quadratic with diffusion-tensor eigenvalues (w.r.t.
  physical q=2πk) **exactly {1/8, 1/8, 1/2} = ratio 1:1:4**, the stiff axis exactly **(1,−1,1)/√3 = the
  C₃ screw / triality axis**. Verified two ways (finite-difference Hessian + independent fresh-adjacency
  directional fits). The dynamical transport is uniaxial along the chiral C₃ axis.

## t08 — the CONSERVED structure of the dynamics (task item 3, previously undeveloped)
Each intrinsic flow has its own conservation law, all forced:
- **Unitary Dirac flow**: COMPLETELY INTEGRABLE per fiber — all 4 band populations |⟨E_j|ψ⟩|² are
  constants of motion (4 commuting charges); energy E_j(k) and crystal momentum k both conserved.
- **Heat flow**: EXACTLY ONE conserved charge = total mass (the unique Laplacian zero mode), with a
  genuine lattice continuity equation ∂_t p + div j = 0 (j_xy = p_x − p_y), verified; everything else relaxes.
- **Geodesic flow**: the periodic-orbit counts N_m = Tr(Bᵐ) (hence the whole Ihara–Ruelle zeta) are the
  conserved conjugacy-invariant data = the flow's resonances.
- **Modular (intrinsic-time) flow**: K = −log ρ is conserved and the KMS state is its stationary fixed point.
- **★ THE TOPOLOGICAL CONSERVATION LAW**: the index **χ = V−E = −2 = str(e^{−tD²}) for ALL t and ALL k**
  — the dynamically protected Noether charge of the Clifford grading {D,G}=0 (McKean–Singer). The ONE
  quantity conserved under all flows, independent of state and fiber. Verified at 4 fibers × 5 times.

## t09 — running forward; the forced/choice ledger of the ARROW (task item 5)
Forward-running is a well-posed Cauchy problem. Sharp forced-vs-choice split:
- **FORCED**: the law (unitary group e^{itD}; contraction semigroup e^{−tD²}); the clock's
  generator/direction (D, D², dΓ(D)); the **arrow on the dissipative flow** — e^{−tD²} is a contraction
  SEMIGROUP (backward heat ill-posed: ‖e^{+5L}‖≈6e11), with a monotone Lyapunov functional / H-theorem
  (relative entropy D(p‖uniform) verified monotone ↓); the unique equilibrium fixed point (= the static
  uniform conserved-mass state), approached at the Laplacian-gap rate.
- **NOT FORCED / CHOICE**: the initial datum (any Cauchy state — needed for a particular *history*); the
  time UNIT/RATE (no intrinsic scale, matching III_1 / T(M)={0}); and the **direction-of-use of the arrow**
  — the unitary Dirac law is itself TIME-REVERSAL symmetric (A(−k)=conj A(k)), so which end is the
  low-entropy "beginning" is set by the initial datum, not the law.

## INTERVALS t07–t09 — the complete INTRINSIC DYNAMICAL theory (broad pass)
Top-down from D, the object's complete dynamical theory: **time/generator** = D (modular flow), intrinsic
& outer only at the second-quantized III_1 level (t03–t06); **what evolves** = states under three flows
(integrable unitary, dissipative heat, hyperbolic geodesic); **conserved** = band populations / total mass
/ orbit-zeta / modular K, all under the master topological charge χ=−2 (t08); **rates** = the forced
dimensionless set {log 2, 1/√2, 2−√2, 2/log 2} + the 1:1:4 C₃-axis diffusion anisotropy (t07); **arrow** =
forced on the dissipative flow, reversible on the unitary law (t09). FORCED: every law, generator,
conservation, rate-RATIO, irreversibility, equilibrium. FREE: the initial state and the single time UNIT
(no intrinsic scale — the object forces all of dynamics EXCEPT one overall scale).

## t10 — the HEAVY sector as the INTEGRATED HISTORY of the running (heavy = cumulative, not local)
Walled; no physics, no fitting. The two high-persistence modes do not wind; their only structure is a
DECAY across slices as the running advances — so their mass content is a history-integrated quantity.
- **History parameter = the C3 screw advance from the symmetric (tracial/hot) Γ start** — = the deck
  generator AND the unique stiff transport axis (1,−1,1)/√3 (t07), = the cooling/modular flow run
  forward. lambda_max(s) is monotone-decreasing 3 → √3 on [0, s_bot]; both heavy modes are functions
  of this ONE coordinate. Small-history law lambda_max(s)=3−c s²+… (Γ is a band top; c≈9.84).
- **Decay profiles, exact:** PERRON |h+|² = 4 → 2, merging into the Ramanujan shell EXACTLY at
  lambda=2√2 (window [0, s_merge≈0.134]); INTER-COPY |lambda_max|² = 9 → 3, bottoming at lambda=√3
  (|.|²=3 exactly) on the C3 axis. Verified.
- **Cumulative persistence** = ∫ log(Ihara-Bass excess over the shell) ds (Bayesian log-return). Over
  the forced window [0,s_merge]: M_perron=0.0728, M_inter=0.1965 → ratio 2.699. Over the inter-copy's
  full window: M_inter=0.4833. Endpoint-free BZ-average exp⟨log(|λ|²/2)⟩=2.516 (a Mahler-type
  constant, no elementary closed form — like the tree entropy).
- **What emerges: a heavy DOUBLET, not a single scale** — two distinct cumulative values because the
  two modes are two different spectral functionals of the same running (inter-copy = the eigenvalue
  |λ|²; Perron = its Ihara-Bass ROOT |h+|², which SATURATES at the merge).
- **★ STRUCTURAL PUNCHLINE: the running is the map v² → v for BOTH modes.** HEAVY (hot/symmetric
  start, accumulated) = the SQUARES {k², (k−1)²}={9,4}; LIGHT (cold end, local single-slice shell) =
  the LINEARS {k,(k−1)}={3,2}. Heavy doublet ratio 9/4; light doublet 3/2; per-mode heavy/light
  (hot/cold) = inter 9/3=k=3, Perron 4/2=(k−1)=2. **All forced by k=3.**
- **FORCED:** the history parameter, both decay profiles (incl. the exact merge lambda=2√2 and bottom
  lambda=√3), the EXISTENCE of two distinct cumulative heavy values, the bare endpoint spectrum
  {9,4}={k²,(k−1)²} over shell (k−1)=2, and all the dimensionless ratios {9/4, 9/2, 2, 3/2}.
  **CHOICE/needs-endpoint:** the OVERALL heavy SCALE (object is scale-free III_1, T(M)={0}; the
  absolute integral depends on the observer's endpoint slice s*) — only RATIOS forced. The cumulative
  AREAS have no elementary closed form. HONEST: the clean forced content is the endpoint spectrum and
  its k-ratios, NOT a single heavy mass.

## t11 — ONE RUNG or a LADDER?  The v²→v square is a SINGLE self-terminating step, not a tower
Walled; no physics, no fitting. Tested whether the t10 square-root map extends into a multi-rung
dyadic ladder (hotter: v⁴→v²; colder: v→√v), against the object's ACTUAL spectrum (not a name-list).
- **Re-verified the one rung:** HOT {9,4}={k²,(k−1)²} = the two Born/persistence weights |λ|², |h+|²;
  COLD {3,2}={k,(k−1)} = the adjacency-Perron λ and the IB shell |h|². v_hot = v_cold².
- **GENERATOR identified:** the square is the Born/modulus map v→|v|², realized concretely by the
  modular β-doubling β→2β (Gibbs weight e^{−βλ} obeys w_{2β}=w_β² exactly). FORMALLY dyadic & uniform
  on the exponent (v^{2ⁿ}); composes in principle.
- **★ UPWARD SELF-TERMINATION (proven):** rung +2 = {81,16}={k⁴,(k−1)⁴} is realized in NO object
  spectrum — it exceeds even the |h|² Born support (which tops at 4). Already rung +1's 9 EXCEEDS the
  adjacency band (max 3) and the |h|² support, living ONLY as the |·|² persistence functional, never a
  literal eigenvalue. So UPWARD the Born map applied a SECOND time has nothing to land on: **exactly
  ONE square step, then stops.**
- **DOWNWARD:** √3, √2, 3^{1/4}, 2^{1/4} all sit in the live band/NB spectrum, BUT do NOT co-occur as a
  matched (k^{1/2},(k−1)^{1/2}) heavy/light PAIR at any single fiber (no fiber carries √3 & √2 together)
  — individual band values, not a coherent lower rung of the same doublet.
- **VERDICT (Q1/Q2):** it is **ONE rung (one square step), NOT a forced multi-rung tower.** The square
  generator (Born = β-doubling) is genuine and dyadic, but the object POPULATES only the single step
  {linears↔squares}; the 2nd square up is empty, the √ down does not reassemble a matched rung.
- **★ WHERE THE HIERARCHY LIVES (Q3):** the forced 2-rung spectrum gives only O(1) ratios {9/4, 3/2, 2,
  9/2}; the cumulative integral M is also O(1). A *doubly-exponential* hierarchy (3/2)^{2ⁿ} would need
  the ladder iterated to rung n≥2 — but those rungs are EMPTY. So within the wall a large hierarchy is
  **NOT manufactured by the forced rungs (a); it is CARRIED BY THE ENDPOINT scale (b)** — the free III_1
  overall scale (T(M)={0}), the observer's endpoint slice s*. Decision: **(b), not (a).**
- **FORCED:** the square generator & its uniform composition law; the two populated rungs {3,2}, {9,4}
  and ratios {9/4,3/2,2,9/2}; the PROVEN absence of rungs ±2 as matched object invariants.
  **CHOICE/endpoint:** which rung is "cold" (β₀) ⇒ the overall scale and ALL of any large hierarchy.

## t14 — THE LARGE-SCALE STRUCTURE OF THE FLOW ITSELF (not a mode in it)
Walled; no physics, no fitting, no adopted target. Develops the large-scale properties the
intrinsic forward-running (dissipative/modular flow from the symmetric hot Γ start) CARRIES.
Builds on t07 (rates, the 1:1:4 tensor), t09 (the arrow), t10/t11 (the v²→v history).
- **GROWTH LAW (forced):** the forward (dissipative) running spreads **DIFFUSIVELY, ⟨r²⟩ ~ t¹**
  (exponent measured 1.007 on a finite srs patch). The unitary Dirac flow is ballistic ⟨r²⟩~t²
  (small-patch fit 1.45→2 with size) but is TR-symmetric and carries no forward arrow, so it does
  NOT govern the forward growth. **Large-scale growth = diffusive, exponent 1, forced by L=D².**
- **FLUCTUATION SPECTRUM (forced):** the acoustic Laplacian band is QUADRATIC (E~|q|², no sound
  term), so the conserved-density fluctuation power is **SCALE-INVARIANT per mode (tilt = 0)** in
  the long-wavelength limit; the ONLY scale dependence is the d=3 density of states ρ(E)~E^{1/2}
  (N(<E)~E^{3/2}, measured d=3.08). No intrinsic peak/tilt — forbidden by III_1 scale-freeness; a
  tilt/peak could only be injected by the FREE initial datum, not by the law.
- **ANISOTROPY (forced, exact):** the acoustic diffusion (band-curvature) tensor is **UNIAXIAL,
  EXACT ratio 1:1:4**, unique axis **(1,−1,1)/√3 = the C3 screw/triality/deck/cooling axis**, perp
  plane isotropic. **★ DIRECTION CORRECTED (inverts the naive "stiff=slow"):** for diffusion
  exp(−E(q)t), ⟨x_n²⟩=2t·E″_nn(0) ∝ band curvature, so the HIGH-curvature C3 axis is the **FAST**
  axis — the density spreads **4× MORE along the chiral screw axis** (real-space ⟨x_axis²⟩:⟨x_perp²⟩
  → 4:1, confirmed by patch: axis/perp rises toward ~4, peak 3.81, then boundary-limited). The
  chiral C3 axis is the PREFERRED growth direction of the running.
- **BUDGET (forced):** signed topological budget = index **V−E = −2**, conserved under every flow &
  fiber (McKean–Singer Noether charge). Recurrent/dispersive mode fraction = **4/10 at the hot Γ
  start → 2/10 generically** (zeta_D(0)=8 dispersive/cell fixed). Geodesic NB budget **1:6:5**
  (growth-Perron : Ramanujan-shell-resonant : tree) over 12 modes, Born weights {4,2,1}.
  Acoustic:optical Laplacian bands **1:3** (the vertex rep 1⊕3).
- **SPECIAL EPOCHS (forced):** modular Schottky heat-capacity peak β_peak≈8.12 (band-width set);
  **Perron-merge horizon s_merge≈0.134** (λ_max=2√2, |h|²: 4→2, the running's first "horizon");
  **inter-copy bottom s_bot≈0.433** (λ_max=√3, |λ|²: 9→3); forced epoch ratio **s_bot/s_merge≈3.23**;
  value landmarks k²→(k−1)²→(k−1) and k²→k (the v²→v square map, t10/t11).
- **FORCED:** growth exponent 1; tilt 0 (+ ρ~E^{1/2}); anisotropy 1:1:4 fast on (1,−1,1); index −2;
  sector splits 1:6:5, 1:3; epoch ratio s_bot/s_merge. **CHOICE/endpoint:** the single overall
  scale / time unit (III_1, T(M)={0}) and the low-entropy initial datum (any tilt/peak/history).
- **★ Verification catch (1, recorded):** first-pass section-(3) prose said the C3 axis was the
  "slow/stiff-spreading" axis; the band-curvature↔real-space-diffusion relation is the OPPOSITE
  (large curvature = fast diffusion). Corrected both in code and here; the patch data always agreed
  with the corrected (fast-on-axis) reading.
