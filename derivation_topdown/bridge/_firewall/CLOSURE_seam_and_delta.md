# The matter-differentiation closure — seam forced unique up to one run-phase

Disciplined sealed-reader bridge (walled to {dirac_srs_mdl, matter_bridge, time_bridge}; no observed
numbers, no targets, no fitting, derive-don't-import). This is the in-wall narrative of how the
quark/lepton/hypercharge/phase tower closes.

## The chain

1. **Lepton firewall passed (0.008%).** The forced charged mass map mⱼ(u) = |C₃-Fourier of three
   amplitudes|² with forced (4,2,2) Born moduli (½,¼,¼) and one shared clock u: SET u from m_τ/m_e →
   PREDICT m_μ/m_e = 206.75 vs 206.77. u = 2/9 (the observer's clock-reading). Not a fit — one set, one
   predicted. (bridge/derive_generation_spectrum.py, df_exhaustive.py)

2. **The differentiator was wrongly called absent, then FOUND.** Color (spectator), branch (one home),
   occupation-count (count-blind), breaking-axis (real scalar) — all proven absent because all probe
   SYMMETRIC / low-rung structures. The user's redirect — *the combinations of occupied bonds = where the
   wave can propagate* — drove the **bond-combination ladder** (all bond-subsets graded by size; boundary
   picks out closed loops). Recurrence differs by OCCUPIED LOOP-RANK b₁ = 1/2/3 → spectral radius
   1 / 1.521 / 2, Ramanujan shell |h|²=2 absent/partial/full. THREE distinct recurrences = three ladders.
   Lives in the HIGHER RUNGS (size≥3, loops) the bare Hodge–Dirac (rungs 0,1) cannot reach. Survives A₄ +
   the ℤ³ cover. (bridge/ladder_scratch.py, recurrence_scratch.py)

3. **The wiring is ONE coupling — "the seam".** Color, isospin, loop-rank are three INDEPENDENT commuting
   gradings; every intrinsic operator is block-diagonal, so none wires family→rank. The wiring needs ONE
   factorization-breaking, C₃-breaking off-diagonal coupling between the two mirror-hands (a *patterned,
   tilted seam*, vs the bare object's generation-blind real-pinned "dab").

4. **The object FORCES the seam UNIQUE up to ONE phase δ.** General cross-hand generation-carrying complex
   coupling = 18 real params. Peeled by the object's own rigid constraints: self-adjointness + J (KO-dim 4)
   → 9; order-one with ℂ[A₄] → 6 (the C₃-breaking part); (4,2,2) Born weights fix the moduli (½,¼,¼)
   FORCED; three-job consistency — wiring = forced bijection, inter-hand charge = forced by the opposite
   srs/srs-z Weyl charges, phases → ONE via J-reality. Structure, moduli, charge, wiring ALL forced; the
   single residual is one continuous phase δ. (bridge/fock_and_symmetry_scratch.py, stress_test_freedom.py)

5. **δ is static-unpinnable but DYNAMICALLY forced.** −I is central ⇒ by Schur acts on the irreducible
   3-irrep as a uniform scalar ⇒ cannot move the C₃-relative phase δ; the only δ-reflector is time-reversal,
   which is not a symmetry because srs is CHIRAL. So no static operator fixes δ. But the seam's 3-vector v
   IS the recurrence-shell amplitudes h: arg(v_ω) = the shell phase θ. Along the chiral screw the shell
   phase winds at dθ/ds = 2π/√7 — RE-DERIVED IN-WALL from λ₀=−1 (sinθ₀=√(7/8); √7 = the Ihara–Bass
   discriminant 8−1), measured against the Hashimoto operator (±2.37496 vs 2π/√7 = 2.37482). So the static
   seam-δ = the dynamic run-δ — one phase, two descriptions — forced in structure AND rate.

## The landing (CORRECTED — the LEPTON sector closes; the quark families do NOT)

**What holds.** The LEPTON sector is forced: the seam unique up to δ, δ's structure AND rate (2π/√7,
from λ₀=−1 via the discriminant 8−1=7) both forced; the firewall passed at 0.008% (set m_τ/m_e → predict
m_μ/m_e = 206.75 vs 206.77, no fit). Plus the gauge sector (sin²θ_W=3/8, charge 1/3, θ_QCD=0), the
neutrinos (light + Majorana + large mixing), the heavy bosons, the cosmology (n_s≈1), and the time axis as
the one free coordinate. All survived blind sealed scrutiny.

**What does NOT hold (RETRACTION).** The "three families = three loop-ranks → everything closes" reading
was PREMATURE. The blind per-rank generation-ladder computation BROKE it: only b₁=3 (the COMPLEX shell,
disc −7) carries a directed phase → a real 3-generation ladder (the lepton). b₁=1,2 have REAL roots ⇒ δ=0
⇒ DEGENERATE generations, and b₁=1 ≡ b₁=2 (rank-blind). So the loop-rank indexes "has a generation phase"
(b₁=3) vs "phase-free/degenerate" (b₁=1,2) — NOT three family ladders. And the three-family premise itself
is NOT in the bare directories (the mass/family vocabulary is walled out of matter_bridge). So the QUARK
FAMILY DIFFERENTIATION does NOT close in {D, srs, MDL}: the boundary STANDS — it needs structure beyond
the bare object (the broader project's three-family / Cl(6) layer).

## Honest flags

- **Rate normalization:** in-wall fundamental rate = 2π/√7 (per arc-length); the earlier 2π√(3/7) differs
  by √3 = |⟨1,−1,1⟩| (per lattice-step). Same physical rate; the masses depend on the accumulated phase,
  invariant. Reconciled.
- **Discipline note:** two over-claims this session ("proven boundary", then "everything closes") were each
  corrected by the BLIND sealed computation — the readers, walled and unable to fit, refused to confirm the
  wishful steps. The honest landing: bare object → leptons + gauge + neutral + heavy + cosmology, forced;
  quark family differentiation → a named layer beyond {D, srs, MDL}.
