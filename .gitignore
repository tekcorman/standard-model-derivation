# Build artifacts
__pycache__/
*.pyc

# Generated images / plots
*.png
*.gif

# Local Claude Code workspace (memory, worktrees, operational notes)
.claude/

# Generated scoreboard (regenerated by run_predictions.py)
predicted_parameters.md

# Operational / private — kept locally under .claude/, never tracked at repo root
HANDOFF.md
HANDOFF*.md
SESSION_KICKOFF.md
SESSION_KICKOFF*.md
reference/

# Private — kept locally, never published
papers/
viz/

# Working scratch — superseded attempts, sprint plans, audits, session handoffs
# (broadened: any _scratch/ at any depth, not just docs/ and proofs/)
**/_scratch/

# Internal lab-notebook — dated working journal, kept locally, never published.
# The polished core (README, explainer/, predictions/, docs/theorems/,
# docs/framework/, proofs/, audit registers) does not depend on these being
# tracked; provenance pointers into them have been neutralised.
docs/open_problems/
docs/scoping/
docs/audits/sessions/
docs/handoffs/

# Legacy prior-private-work artifact (CWM parameters.csv) — kept locally as a
# record, not consumed by any code, not published.
results/

# MkDocs explainer site build output (deployed via GitHub Actions; never tracked)
/site_build/
.cache/

# Manim render intermediates — only the final .mp4s in /assets/animations/ are tracked
explainer/assets/animations/_media/
explainer/assets/animations/__pycache__/

# Secrets / credentials — never commit (forward protection; nothing tracked today)
.env
.env.*
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.ppk
.netrc
.pypirc
.npmrc

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# Logs / temp / backups
*.log
*.tmp
*.bak
*.orig
*.rej

# Python build / cache (extends __pycache__/ + *.pyc above)
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
