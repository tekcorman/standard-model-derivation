# State of the absolute scale — the target shot, RETRACTED & corrected (2026-05-17)

> **RETRACTION BANNER (2026-05-17, same day).** The first version of
> this doc (and the target-shot probe's TS-A4/VERDICT prose) concluded
> the absolute scale was *"one unit definition + one irreducible epoch
> coordinate; N_hub = the age = 'now'; provably not closeable from
> within; the dimensional/epoch floor every theory has; not a research
> target."* **That interpretation is RETRACTED as an overclaim.** The
> user correctly punctured it: a dimensionless substrate tick/hub count
> fixed by *derived* toggle dynamics is **not** "what time is it in
> seconds"; whether it is pinned by a substrate self-consistency is an
> **open, bounded technical question (Gap G1)**, not a metaphysical
> floor. The probe's *verified facts* (Steps TS-A1..A3, A5 numerics)
> stand; only the *epoch-floor interpretation* was wrong. This doc is
> the corrected record. (Same discipline as the b1' instrument
> correction — an overclaim left in the record is retracted, not
> defended.)

**Purpose.** Honest statement of what the framework's absolute scale
is, after the decompose→b0→b1→b1' arc + the target shot + this
correction. Probe: `proofs/foundations/target_shot_absolute_scale_2026-05-17.py`
(verified facts stand; interpretation banner-corrected). Read after the
convergence capstone (`state_of_the_derivation_2026-05-16.md`).

---

## 1. The result (corrected, one sentence)

The framework's absolute scale is **fully determined in
substrate-natural units with zero free parameters and *derived* (not
adopted) dynamics — except for the *current value* of one pure integer,
N_hub, which is the single remaining open input (Gap G1): a
sharply-posed, bounded target, neither closed nor proven impossible.**

## 2. What is verified (the target-shot facts — these stand)

| Piece | Status |
|---|---|
| `M_Pl/M_substrate = 8/√π`; `M_substrate ≡ e_bit ≡ 1`; `t_tick = 2π`; GeV = one declared conventional choice | **exact theorem / definition; zero fitted** |
| distance tied (`cell = 1`, `c = 1`) | definition/theorem |
| **toggle dynamics quantified & tied to N**: `p_toggle = 1/(k*·N)` from k\*=3 + A1 (NO adoption); `H = 1/(N·t_P)` coeff exactly 1; growth law `dN/dt = h·N/t_P` | **THEOREM-GRADE, derived** (`N_hub_spectral_gap_attempt.py` Steps A–D; the user's point — this was *understated* before) |
| `N(t) = t/t_P` cascade; `Λ_substrate = 1/N²` | theorem, **but N-derived** (Λ from Friedmann + H=1/(N·t_P): *not* an independent handle on N) |
| N_hub ≈ 8.39×10⁶⁰ over-determined (G_F-consistency ppm vs H₀ "universe size" ~1.15 %) ⇒ ONE shared number | verified — one number, not per-observable fits |

So in natural units **everything is parameter-free and the dynamics is
derived, except one pure integer's current value.**

## 3. What was overclaimed, and the corrected status

**Overclaimed (retracted):** "N_hub = the age = 'now'; no theory
derives 'now'; provably not closeable; the epoch floor; not a research
target." This imported an anthropocentric "what time is it in seconds"
intuition onto a *dimensionless* substrate count.

**Corrected status (the framework's own, verbatim, from
an internal working note, 2026-04-21):** the
**FORM/relation is derived/CLOSED** (toggle dynamics, `H=1/(N·t_P)`,
growth law, de-Sitter structurally available from srs-Ramanujan); the
**current epoch value of N is BLOCKED/OPEN** — *"requires an
independent derivation of the current N from the cosmological history
(inflation end + expansion) … the discrete Gauss-Codazzi theorem."*
The rate law is **scale-invariant in N** (identical at N=10⁶⁰ and
10⁸⁰): it gives the *trajectory*, not the *point on it*. `Λ=1/N²` is
**derived from N**, so it is *not* an independent constraint that could
pin N (the circularity is real).

⇒ Gap G1 is **OPEN and BOUNDED**, not a floor and not closed. The
single missing ingredient is sharply identified: a substrate-internal
condition that selects a *particular* N (a boundary/origin or
self-consistency), since the scale-invariant rate dynamics provably
cannot, and Λ supplies no independent handle.

## 4. The corrected forward target (and an important correction to a
second overclaim)

The genuine remaining target is **Gap G1: derive the current N from the
substrate alone.** Two earlier overclaims are corrected:

1. *"the deep-layer program with no bounded entry"* (stated twice
   earlier, twice corrected by the user) — Gap G1 is **bounded and
   sharply posed**: one integer, one selection condition to find.
2. *"a substrate value for N_hub would be the numerology
   anti-pattern / producing the number is the failure"* — **this
   conflation is corrected.** A *fitted or coincidental* match to
   8.39×10⁶⁰ would be numerology. A **principled substrate
   self-consistency** that derives N with pre-declared structure and
   zero observational input would be exactly the legitimate Gap-G1
   closure — *not* numerology. The target-shot probe's TS-A5 framing
   ("producing the number = failure") pre-judged this and is
   superseded by the N-waterline probe (§5), which tests for the
   *principled* route and forbids only the *fitted* one.

## 5. The N-waterline shot (Gap-G1 epoch-selection) — RESULT

`proofs/foundations/n_waterline_epoch_selection_2026-05-17.py`
(all aborts passed) → **CHARACTERIZED HONEST NEGATIVE**, disciplined
in both directions:

- **Enumeration (NW-A1):** every substrate-internal quantity is class
  (i) — N-independent structural constant (k\*, g, α₁, ε_CP, the
  Cheeger/spectral gap; cannot *select* an N) — or class (ii) — derived
  *from* N (H, `Λ=1/N²`, p_toggle, S_total, MDL capacity; circular).
  **Zero class-(iii)** (N-non-scale-invariant *and* N-independent).
- **Λ (NW-A2):** no independent structural Λ; `Λ=1/N²` is N-derived
  (Friedmann), so no shortcut `N=1/√Λ`.
- **MDL fixed-point (NW-A3, the user's hypothesis):** the natural
  self-consistency `S_total(N) = capacity(N)` is **scale-invariant** —
  both ∝ N¹ (b1': per-tick surprise bounded ⇒ S_total ∝ tick count;
  capacity ∝ N hubs) — so it pins **no unique N**.

⇒ **The N-waterline hypothesis is, as stated, refuted** (honest
route-elimination, Need-B-style — *not* a failure, and *not* the
retracted "epoch floor"). Gap G1 is therefore **not** a
fixed-point-in-N problem. It reduces precisely to the framework's
already-documented block: a derived **walk-origin / boundary
condition** — N₀ at "inflation end" plus elapsed expansion, i.e. the
**discrete Gauss–Codazzi cosmological-history closure**
(`N_hub_spectral_gap_attempt.py`). That is a well-defined, **open,
bounded** target of a *different type* than an internal
self-consistency.

## 6. The discrete Gauss–Codazzi walk-origin shot — RESULT

`proofs/foundations/gauss_codazzi_walk_origin_2026-05-17.py`
(all aborts passed) → **CHARACTERIZED HONEST NEGATIVE + SCOPING**,
disciplined in both directions (explicit anti-overclaim self-check):

- **GC-A1 (from KNOWN math):** Gauss–Codazzi = the Hamiltonian/momentum
  *constraint* equations; the FRW Hamiltonian constraint *is* the
  framework's Friedmann relation `Λ=1/N²`, which is an identity along
  the trajectory (`H²−Λ≡0 ∀N`) — **scale-invariant in N**. Constraint
  equations *relate* Cauchy data on a slice; by their nature they do
  **not generate** it (Wald §10). ⇒ the epoch N is a **cosmological
  Cauchy datum**; this is the N-waterline scale-invariance seen in its
  true form.
- **GC-A2 (route-elimination, falsifiable):** the only principled
  absolute-N pin is a discrete topological quantization the smooth
  constraint lacks. The framework's *sole* discrete index (substrate
  Atiyah–Singer) is the **N-independent chirality/anomaly index**
  (3 gens, ν_R, sin²θ_W=3/8) — it constrains spinor structure, not
  substrate size. So that route is **structurally unavailable** with
  current structure (not "impossible in principle" — a specific,
  falsifiable framework finding).
- **GC-A3:** the discrete Gauss–Codazzi theorem proper (Cheeger
  `h=O(1)` ↔ a discrete Friedmann constraint generating `N₀`) is
  **unbuilt new mathematics — the framework's own estimate ~6–12
  months**. Named and scoped here, **not derived/faked**.
- **GC-A5 (discipline self-check):** the verdict is scanned for and
  contains **no** floor-overclaim tokens. I asserted "irreducible /
  epoch floor" twice this session; this is the explicit guard against
  a third. The result does **not** adjudicate whether N is ultimately
  derivable or a primitive initial condition.

## 7. Disposition (corrected, final for this session)

The absolute scale is closed in natural units with **derived
dynamics**, down to one open integer (Gap G1) whose status is now at
its honest, both-directions-disciplined terminus:

- **Not closed** (no closure manufactured): the discrete Gauss–Codazzi
  theorem is unbuilt new math (~6–12mo, framework's estimate).
- **Not a floor** (retracted, and not re-asserted — GC-A5): the
  question is not declared irreducible.
- **Sharply localized:** Gap G1 = the **cosmological Cauchy datum /
  walk-origin N₀**. The internal fixed-point route is refuted (§5,
  scale-invariance) and the discrete-topological-pin route is
  eliminated (§6, the only index is N-independent). The single
  remaining route is **named and scoped**: the ~6–12-month discrete
  Gauss–Codazzi mathematics — *or* N₀ is supplied as a cosmological
  initial condition. **Which of these obtains is deliberately not
  adjudicated** (adjudicating it either way would repeat the
  retracted overclaim).

That is the honest end state of the "missed the target" arc: the
entire absolute scale reduced — with fully derived evolution dynamics
— to one open integer, two candidate closure-routes refuted/eliminated
with structural reasons, and the one surviving route named, scoped, and
left honestly open without an irreducibility claim.

Cross-refs: `state_of_the_6i_mass_identification_thread_2026-05-17.md`
(§§3,5 corrected), `state_of_the_derivation_2026-05-16.md` (pointer
corrected), an internal working note,
`predictions/N_hub.py`, `predictions/Lambda_CC.py`.
