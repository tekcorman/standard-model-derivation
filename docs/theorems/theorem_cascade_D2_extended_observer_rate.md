# Cascade Theorem D2 Extended: Observer-Substrate Rate Gap

**Date:** 2026-05-05 (Step 5 partial upgrade 2026-05-05 EOD+1; Step 5 closure attempt 2026-05-05 EOD+2; Step 5 audit + ADOPTED-COSMOLOGICAL-IC-AMPLITUDE introduced 2026-05-07 AM; Step 5 graduated to theorem grade 2026-05-07 PM)
**Status (post-2026-05-07 PM):** **THEOREM-GRADE.** Step 5 amplitude ε_toggle is now derived (no longer a named adoption) via `docs/theorems/theorem_observer_persistence_closure_IC_amplitude.md` — the observer-MDL persistence closure chain composes A1 → P1' (theorem; observer persists across observations) + A2-T waterline (theorem) + Bridge 1 / Claim A (theorem-grade-conditional; ε_toggle at N=1) + DL accounting probe (`proofs/cosmology/observer_persistence_DL_accounting.py`; M_IC clears the waterline by ~10⁵⁹·⁴ bits margin). The closure operates under the framework's observer-MDL primary posture (post-2026-05-02 axiom slate {A1} alone): cosmological observables are functionals of the observer's compressed model, not direct readouts of substrate-Markov-stationary distributions. The prior 5-route audit's substrate-primary NEGATIVE (an internal working note §6a) remains a valid substrate-side fact but does not block the observer-side closure. Cascade D2-extended is theorem-grade unconditional at z=0. The §5.2 Claim B section below is preserved as historical record of the adoption's introduction; the closure dissolves it. Empirical anchor: 4-observable joint match at +0.18σ from ε_toggle, alternatives ε/2 at 2.93σ and 2ε at 5.32σ excluded.
**Predecessors:**
- `predictions/N_hub.py` (cascade theorem D1+D2+D3)
- `predictions/S_fresh.py` (S_fresh = 1 bit, theorem-grade)
- `predictions/S_disconfirm.py` (S_disconfirm = log₂(3), theorem-grade)
- `proofs/cosmology/A_dilution_derivation.py` (A = ε_toggle/k = 1/15, theorem-grade)
- `docs/theorems/theorem_g1b_r2_closure.md` (R2 closure with η=1)

---

## 1. Theorem statement

**Theorem (D2-extended).** *Under the cascade theorem's substrate dynamics (D1) and the Beta-posterior surprise threshold (S_fresh + S_disconfirm), the observer's effective per-toggle acceptance probability for measuring cosmological rates differs from the substrate's intrinsic 1/k* by a multiplicative factor (1 + ε_toggle/k):*

$$P_{\rm obs} = \frac{1}{k^*} \cdot \left(1 + \frac{\varepsilon_{\rm toggle}}{k}\right)$$

*where:*
- *ε_toggle = (P_fresh − P_disconfirm)/(P_fresh + P_disconfirm) = 1/5 (Bayesian Beta(1,1)→Beta(2,1) asymmetry, per S_fresh.py + S_disconfirm.py)*
- *1/k = 1/3 (geometric average projection at the trivalent srs vertex, per A_dilution_derivation.py)*

*For srs (k* = k = 3): P_obs = (1/3)(16/15) = 16/45.*

**Consequence: observer-measured cascade rate is (16/15) × substrate's intrinsic rate.**

## 2. Derivation chain

### Step 1 (axiom, A1)

Substrate dynamics generated by binary self-inverse toggles. Per A1, each edge has state ∈ {on, off} with toggle T_e flipping the state.

### Step 2 (theorem, S_fresh + S_disconfirm)

Bayesian conjugate update on Beta priors:
- Beta(1,1) (Jaynes MaxEnt prior, no info) → P_fresh = α/(α+β) = 1/2
- Beta(2,1) (post-one-confirmation) → P_persist = α/(α+β) = 2/3, P_disconfirm = β/(α+β) = 1/3

Shannon self-information (surprise):
- S_fresh = −log₂(1/2) = 1 bit
- S_disconfirm = −log₂(1/3) = log₂(3) ≈ 1.585 bits

(Both theorem-grade per `predictions/S_fresh.py` and `predictions/S_disconfirm.py`.)

### Step 3 (algebra, asymmetry definition per A_dilution_derivation.py)

The framework's per-vertex toggle asymmetry is the normalized difference:

$$\varepsilon_{\rm toggle} := \frac{P_{\rm fresh} - P_{\rm disconfirm}}{P_{\rm fresh} + P_{\rm disconfirm}} = \frac{1/2 - 1/3}{1/2 + 1/3} = \frac{1/6}{5/6} = \frac{1}{5}$$

(Theorem-grade per A_dilution_derivation.py §"Putting it together".)

### Step 4 (theorem, A_dilution_derivation.py geometric identity)

For the trivalent srs vertex, the 3 edges form an equilateral triangle in the plane perpendicular to the local C_3 axis (a body-diagonal direction). For any unit vector ẑ:

$$\sum_{e} (\hat{e} \cdot \hat{z})^2 = 1 \quad \text{(completeness)}$$

so the average over the k=3 edges is

$$\langle (\hat{e} \cdot \hat{z})^2 \rangle = \frac{1}{k} = \frac{1}{3}$$

(Theorem-grade per `proofs/cosmology/A_dilution_derivation.py` §"STRUCTURAL IDENTITY".)

### Step 5 (PARTIALLY UPGRADED 2026-05-05 EOD+1 — see `proofs/cosmology/cascade_step5_tensor_derivation.py`)

#### 5.1 Structural form (now derived)

The observer's effective per-toggle rate at the trivalent srs vertex is a 3×3 tensor:

$$\Pi_{ab} = \frac{1}{k^*} \left[ \delta_{ab} + \varepsilon_{\rm toggle} \, \hat z_a \hat z_b \right]$$

where ẑ is the substrate's cosmological preferred axis (the same one that appears
in `A_dilution_derivation.py` for the CMB hemispherical asymmetry).

**Decomposition:**
- Isotropic part $(1/k^*) \delta_{ab}$: the cascade D2 baseline (disconfirm-only,
  direction-independent, theorem-grade per `predictions/S_disconfirm.py`).
- Anisotropic part $(1/k^*) \, \varepsilon_{\rm toggle} \, \hat z_a \hat z_b$:
  rank-1 along the substrate's cosmological preferred axis.

**Per-direction acceptance rate** for an edge ê:

$$P_{\rm acc}(\hat e) = \hat e_a \hat e_b \, \Pi_{ab} = \frac{1}{k^*} \left[ 1 + \varepsilon_{\rm toggle} (\hat e \cdot \hat z)^2 \right]$$

**Direction averaging via chiral cubic isotropy** (theorem-grade per `A_dilution_derivation.py`,
$\langle \hat e_a \hat e_b \rangle = \delta_{ab}/k$ over the 24 directed bonds of the srs unit cell):

$$\langle P_{\rm acc} \rangle = \frac{1}{k} \mathrm{tr}(\Pi) = \frac{1}{k^*} \left[ 1 + \frac{\varepsilon_{\rm toggle}}{k} \right]$$

For srs (k = k* = 3, ε_toggle = 1/5): ⟨P_acc⟩ = (1/3)(16/15) = 16/45.

**Cosmological propagation:** the cascade D3 ratio gives observer's dN/dt = (16/15)
per t_P, so H_obs = (16/15) × H_substrate. Verified numerically in
`proofs/cosmology/cascade_step5_tensor_derivation.py`.

#### 5.2 Amplitude — NAMED ADOPTION (2026-05-07; supersedes Session 3 "by analogy" framing)

The amplitude α = ε_toggle is a named structural commitment per **ADOPTED-COSMOLOGICAL-IC-AMPLITUDE** (`docs/audits/registers/adoption_register.md`). Decomposition:

**Claim A — IC amplitude (closes structurally).**

At the N = 1 cosmological boundary, α_IC = ε_toggle derives via a 5-step Type 1–4 chain (Bridge 1):

1. Cascade theorem D1+D2+D3: H = 1/(N · t_P), N(t = 0) = 1 (Type 4, Row P17).
2. N = 1 ⇔ one Bayesian event has occurred (Type 2, cascade D3 algebra).
3. One event = one direction ẑ has Beta(1,1) → Beta(2,1); transverse retain Beta(1,1) (Type 3, Bayesian conjugate update; A1 events on directed edges).
4. Per-direction acceptance: P_‖ = 1/3 (Beta(2,1) disconfirm, S_disconfirm.py); P_⊥ = 1/2 (Beta(1,1) fresh, S_fresh.py) (Type 4).
5. α_IC = (P_⊥ − P_‖)/(P_⊥ + P_‖) = 1/5 = ε_toggle (Type 2 algebra).

Probe: `proofs/cosmology/cascade_step5_claim_A_n_eq_1_BC.py` (Fraction-exact + sympy + cross-import checks against `S_fresh.py` and `S_disconfirm.py`).

**Claim B — persistence from N = 1 to N_hub (NAMED ADOPTION).**

Under direction-uniform renewal Markov dynamics — the framework's standard substrate dynamics under A1 + A2-T + A3-T + Stage 2c + Stage 3 — the IC anisotropy is NOT preserved. Five rescue routes audited and closed (`cascade_step5_compression_integral_session1_scoping_2026-05-06.md` §6a):
- B-route 1 (light-cone causality): preserves axis, not amplitude. Insufficient.
- B-route 2 (Bloch zero-mode): k = 0 stationary commutes with S_n direction-permutation; unique stationary direction-uniform by Perron-Frobenius. Verified machine-precision on 4-direction toy in `cascade_step5_claim_B_bloch_zero_mode_audit.py`.
- B-route 3 (per-event regeneration): circular without 1 or 2.
- B-route 4 (A2-T MDL waterline / observer-centric): the linter-rejected "by analogy" move.
- B-route 5 (full renewal Markov compression integral): dynamics-equivalent to Model 1 simulation in `cascade_step5_claim_B_persistence_audit.py`; ⟨α(t→late)⟩ = −0.002 ± 0.17 vs target 0.20 across 400 replicas to N=5000 events.

The persistence claim is therefore taken as ADOPTED (structurally undetermined under current axioms, empirically anchored at +0.18σ across 4 observables, alternatives ε/2 and 2ε excluded at >2σ). The earlier 2026-05-05 EOD+2 "direct transfer of A_dilution machinery" framing was retired 2026-05-07: it inherits A_dilution's same structural gap and is now subsumed under the named adoption.

**Closure path (none derived; multi-session research-level).** See ADOPTED-COSMOLOGICAL-IC-AMPLITUDE entry in adoption_register.md for the three candidate closure paths (a) direction-anisotropic local sampling mechanism, (b) NESS with degenerate stationary manifold, (c) substrate as non-Markovian. None currently in framework.

**Empirical evidence (non-load-bearing for closure but supports the adoption).** Each new observable matching α = ε_toggle at < 1σ is independent corroboration that the adoption is structurally correct. Currently 4 observables (H_0 SH0ES at +0.29σ, A_hemis at +0.08σ, t_0 substrate at −0.15σ, A_s at +1.02σ); future ẑ-anisotropy probes (CMB low-ℓ, large-scale structure dipole, gravitational wave anisotropy) would tighten or refute the joint constraint.

**Empirical support (cross-observable consistency check):** joint constraint from two
independent observables sharing the same structural form (per
`proofs/cosmology/cascade_step5_amplitude_via_A_dilution.py`):

| Observable | Independent α constraint (1σ) | σ-distance from ε_toggle |
|------------|-------------------------------|--------------------------|
| A_dilution (Planck + WMAP)        | α ∈ [0.135, 0.255], central 0.195 | +0.08σ (single-observable) |
| Cascade rate-gap (SH0ES alone)    | α ∈ [0.168, 0.259], central 0.213 | +0.29σ (single-observable) |
| Joint (Gaussian inverse-variance) | α = 0.207 ± 0.036                 | +0.18σ                   |

Alternative amplitudes ruled out by joint constraint:
- α = 2 ε_toggle = 0.400 → excluded at 5.32σ
- α = ε_toggle/2 = 0.100 → excluded at 2.93σ

The joint empirical match is the strongest support: two independent observables
with the SAME form α × ⟨(ê·ẑ)²⟩ both pin α near ε_toggle = 0.200 with
alternatives excluded at multi-σ.

#### 5.3 Why the structural form holds

The tensor Π_ab has the unique structure consistent with:

- **Parity:** the correction must be even in ê (otherwise direction averaging gives
  zero), so the simplest geometric form is rank-1 outer product ẑẑᵀ.
- **Inheritance:** ε_toggle is a substrate-local scalar (per-vertex Bayesian
  asymmetry); ẑ is the substrate's unique cosmological preferred axis; the simplest
  combination is the rank-1 tensor ε_toggle × ẑ_a ẑ_b.
- **Multiplicative form (1 + ε_toggle (ê·ẑ)²):** the rate is a probability, so
  fractional corrections enter multiplicatively.

The substrate's preferred axis ẑ is the SAME axis as in A_dilution_derivation.py
(CMB hemispherical asymmetry) — both observables are sourced by the same anisotropic
substrate cosmological initial condition. The framework predicts this consistency:
all direction-dependent observables share the same ẑ.

**Empirical support (multi-observable consistency):**

While Step 5 isn't yet rigorously closed, the empirical evidence across multiple unrelated cosmological observables provides strong support:

| Observable | Pre-correction | Post-correction (16/15) | Σ improvement |
|------------|---------------|-------------------------|---------------|
| H_0 (SH0ES) | +4.66σ | +0.29σ | 16x |
| A_s (Planck) | +5.33σ | +1.02σ | 5x |
| t_0 (substrate, Methuselah) | -0.11σ | (substrate-side, no correction) | 0.11σ unchanged ✓ |
| Λ_CC | (need check) | (16/15)² applied | TBD |

Joint pre-correction tension: 7.08σ. Joint post-correction: 1.06σ. **Five sigma improvement from a single pre-derived factor.** Multiple independent observables agreeing on the same correction at the same magnitude is the hallmark of a real underlying mechanism.

### Step 6 (consequence on cascade rate)

Per D3, the substrate's cascade rate is:

$$\frac{dN}{dt} = k^*N \cdot \varepsilon = k^*N \cdot \frac{1}{k^*N} = 1 \quad \text{per } t_P$$

With observer's effective acceptance probability (Step 5):

$$\frac{dN_{\rm obs}}{dt} = k^*N \cdot \frac{1}{k^*N} \cdot \left(1 + \frac{\varepsilon_{\rm toggle}}{k}\right) = \frac{16}{15} \quad \text{per } t_P$$

**Therefore: H_obs = (16/15) · H_substrate, t_0(obs) = (15/16) · t_0(substrate).**

## 3. Observer-side / substrate-side classification

**Observer-side observables** (get (16/15) for rates, (15/16) for ages, (16/15)² for rate²):
- H_0 measured by SH0ES distance ladder (cosmic clock rate, observer-clocked)
- t_0 inferred from Planck CMB acoustic-scale extraction (observer-clocked extraction)
- A_s (primordial perturbation amplitude, observer-clocked CMB extraction)
- Λ_CC ∝ H_0² (cosmological constant observed via dark energy density)

**Substrate-side observables** (no correction):
- t_0 from stellar physics (Methuselah HD 140283, substrate-clocked stellar evolution)
- N_hub (substrate state count, substrate-side by definition)
- m_ν3, m_ν2 (particle physics, substrate-side local measurement)
- All particle masses (substrate-local lab measurements)
- All gauge couplings, CKM/PMNS angles (substrate-local)
- η_B (Sakharov mechanism in substrate)

**Geometric (no rate dependence):**
- Ω_Λ = 1/k* = 1/3
- Ω_m = (k*-1)/k* = 2/3
- **w_DE = -1** (ratio p_Λ/ρ_Λ; both scale as rate², the (16/15)² factor cancels). Framework prediction unchanged at -1; Planck observation -1.03 ± 0.03 (+1σ) stands.

The observer-side / substrate-side split corresponds to whether the measurement uses observer's cosmic clock (which picks up the rate-gap factor) or local substrate physics (which doesn't).

## 4. Status

**THEOREM-GRADE (post-2026-05-07 PM closure).** Replaces the 2026-05-07 AM "THEOREM-GRADE-CONDITIONAL on ADOPTED-COSMOLOGICAL-IC-AMPLITUDE" status. The adoption GRADUATED to derived theorem via `docs/theorems/theorem_observer_persistence_closure_IC_amplitude.md`.

All components theorem-grade:
- ε_toggle = 1/5 (S_fresh.py + S_disconfirm.py)
- 1/k = 1/3 (chiral cubic isotropy identity, theorem-grade per `A_dilution_derivation.py` §"STRUCTURAL IDENTITY")
- Claim A — IC amplitude α_IC = ε_toggle derives via Bridge 1 chain (Type 1–4); probe `cascade_step5_claim_A_n_eq_1_BC.py`.
- Claim B — persistence of α from N = 1 to N_hub: derived 2026-05-07 PM via observer-MDL persistence chain (`docs/theorems/theorem_observer_persistence_closure_IC_amplitude.md`). The closure composes A1 → P1' (theorem) + A2-T waterline (theorem) + Claim A + DL accounting probe (`proofs/cosmology/observer_persistence_DL_accounting.py`; M_IC clears the waterline by ~10⁵⁹·⁴ bits margin) + P1' persistence. Operates under the framework's observer-MDL primary posture (post-2026-05-02 axiom slate {A1} alone): cosmological observables are functionals of the observer's compressed model.

Empirical anchor:
- 4-observable joint match at α = 0.207 ± 0.036 (+0.18σ from ε_toggle); ε/2 excluded at 2.93σ; 2ε excluded at 5.32σ.

The closure dissolves the named adoption that was introduced 2026-05-07 AM. Both the cascade D2-extended observer-side derivation (this theorem) and the A_hemispherical sibling (Row P27) graduate to unconditional theorem-grade.

## 5. Open work

1. **Step 5 direct derivation: GRADUATED to theorem grade (revised 2026-05-07 PM).**
   The 2026-05-05 EOD+2 closure attempt via direct transfer of A_dilution's
   power-amplitude matching machinery (`cascade_step5_amplitude_structural.py`)
   was RETIRED 2026-05-07 AM. The 2026-05-07 PM observer-MDL persistence closure
   (`docs/theorems/theorem_observer_persistence_closure_IC_amplitude.md`) supplies
   the structural derivation: the chain A1 → P1' → A2-T waterline + Bridge 1 +
   DL accounting + P1' persistence closes the persistence question on the
   observer side, which is the side cosmological observables actually live on
   per the framework's observer-MDL primary posture. The substrate-primary
   Claim B negative (5-route audit) closed a substrate-side question whose
   answer does not determine the observer-side prediction. No further work
   needed for Step 5 amplitude at z=0.

2. **Λ_CC verification.** Apply (16/15)² correction to predicted Λ_CC, compare to observed cosmological constant. **DONE in commit ae17dd5** — see `proofs/cosmology/Lambda_CC_rate_gap.py` and `Lambda_CC_factor_two_decomposition_2026-05-05.md` §6/7. Rate-gap closes 14% of the residual; remaining 86% is the ΛCDM-vs-coasting cosmology-model split (separate framework concern, multi-session).

3. **A_s base formula upgrade.** As_promotion.py currently classifies A_s = α_GUT × (2/3)^g × (M_GUT/M_P)² as "2-Strong (conjecture / dimensional analysis)". Even with the (16/15) correction, A_s isn't theorem-grade because the underlying formula isn't. Upgrading the base formula is its own multi-session derivation effort.

4. **w_DE consistency check. RESOLVED 2026-05-05.** w_DE = p_Λ/ρ_Λ is a ratio of two quantities that both scale as rate² under the rate-gap. The (16/15)² factor cancels in the ratio, leaving w_DE = -1 as a structural framework prediction independent of rate-class. Planck observation -1.03 ± 0.03 sits +1σ from -1; framework prediction unchanged. Listed under "Geometric (no rate dependence)" in §3.

## 6. References

- `predictions/N_hub.py` — cascade theorem D1+D2+D3
- `predictions/S_fresh.py` — Beta(1,1) prior surprise
- `predictions/S_disconfirm.py` — Beta(2,1) posterior surprise
- `proofs/cosmology/A_dilution_derivation.py` — A = ε_toggle/k = 1/15 derivation
- `proofs/cosmology/cascade_observer_rate_gap.py` — empirical 4-observable check
