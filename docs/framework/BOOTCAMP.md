# BOOTCAMP — mandatory foundation for any fresh context (written 2026-07-10 with the framework live)

**This is FOUNDATION, not status.** Status lives in internal research notes (the
parameter-complete register) and `docs/incomplete_equations_todo.md` (the working queue). Read this
file FIRST, then those two. Every claim here has a pointer; where this file and a pointer disagree,
the pointer wins and the disagreement gets flagged.

## §0 THE STANDING LAWS (permanent, user-enforced)
1. **TOP-DOWN:** every number is a FORCED READ of one object's spectrum. No fitting, no numerology.
   A value that doesn't fall out top-down = an INCOMPLETE DEFINING EQUATION: locate it, log it in
   the todo, chase the math UP. An open miss stays open — never relabeled.
2. **ONE-OBJECT:** physics = the triple (D, ω, {A(O)}). Layer-1 math accretes in
   `derivation_topdown/bridge/the_run.py`; Layer-3 in `derivation_topdown/state/the_net.py`.
   EXTEND, never fork; never rebuild what the master objects expose.
3. **MODEL DISCIPLINE (permanent):** architect = architect only (pre-reg freezes, verdict adjudications,
   user decisions). EVERY helper launch sets an explicit cheaper model (`a model` implementation passes/
   checkers/sweeps, `a model` mechanical). No override = forbidden. Max 2 concurrent without the
   user's batch approval; state model + count before launching. Heavy waves from fresh sessions.
4. **PROCESS:** effort scales with goal-seek risk (LIGHT architect-direct / MEDIUM implement+check /
   HEAVY sweep→pre-reg→implement→adversarial-check→integrate). Pre-regs freeze verdict trees BEFORE
   computation; dual-outcome always; sealed-agent protocol when the architect has seen the data.

## §1 WHAT THE THEORY IS
One finite combinatorial object generates the dimensionless skeleton of the Standard Model +
cosmology as its spectrum. D = B(srs⊗srs-z) ⊗ ∂_N: B is the non-backtracking (Hashimoto) walk
operator on the srs net; ∂_N is the tick/counting direction. The STATE ω (Layer 2) makes it physics:
the run is a KMS state and time is its modular flow. The NET {A(O)} (Layer 3) is the one partially
built object; every deep open item is a face of it. Pointers: `docs/framework/framework_axioms.md`,
`docs/framework/three_layers.md`.

## §2 THE OBJECT (and the two traps that misidentify it)
- **srs**: the chiral trivalent 3D net (space group I4₁32, Wyckoff 8a parameter-free), girth 10,
  the unique (3,10)-cage; primitive cell = K4 quotient, 12 darts.
- **SELECTION (corrected 2026-06-15/07-10 — do NOT cite the old story):** srs is **DOMINANT, not
  unique** — selected by the structural-fingerprint + MDL-waterline study over the nine
  V+E-transitive chiral candidates (survivors {srs, srs-c8, lou, lov}; selection priced at 2.0 bits).
  Sunada's strong-isotropy theorem is TRUE but carries NO selection load (srs-z IS arc-transitive —
  the old uniqueness chain conflated arc-transitivity with strong isotropy). Authoritative:
  R-9 SUPERSESSION in `docs/audits/registers/structural_residue_register.md` (~line 160) +
  internal research notes (12 draft-ready statements).
- **TRAP:** srs-z = the BODY-CENTERING bipartite double of srs (a folding), NOT the mirror
  enantiomer. The enantiomer is I4₃32, a different thing entirely. Never conflate the two Z₂'s.
- **The walk is not chosen:** A1's involutive toggle words MDL-canonicalize to reduced words =
  NB walks (Serre); the minimal causal state is the directed edge (1 bit) ⟹ the one-step operator
  is FORCED to be Hashimoto B. Ihara–Bass binds spec(B) to the adjacency spectrum.
  Pointer: `proofs/foundations/walker_dynamics_derivation.md` (NOTE its Step 4b carries a
  2026-07-10 supersession banner — respect it).

## §3 THE FORCED CONSTANTS (the spine of every read)
q_NB = (k−1)/k = 2/3 per-step survival · **α₁ = (2/3)⁸** (girth 10 ⟹ g−2 = 8 constrained steps) ·
at the P point (the physics point: C3 generations, Koide, the mass machinery) the walker root
**h = (√3+i√5)/2**, |h|² = 2 = k−1 (Ramanujan saturation); k* = 3, d = 3, girth 10, p_toggle 2.
**TRAP (the #1 conflation):** α₁ is the internal walk fugacity. It is NOT α_EM (≈1/128 at M_Z,
derived separately from α_GUT = 1/24 + sin²θ_W = 3/8 + RG). Anything mixing them is wrong.

## §4 LAYER 2 — THE STATE, TIME, AND THE TEMPERATURE DICTIONARY
The run |G⟩ = Σ uⁿBⁿ|seed⟩ at u = α₁ **is the KMS state of the tick**: thermal time = the tick;
the arrow = sub-criticality u < u_c = 1/(k−1) = 1/2; Landauer lock β·κ = ln 2; κ = h/t_P (N̂ integer
⟹ U(1) period 2π ⟹ one tick = h). The dart algebra is CONSTRUCTED (2026-07-10): Cuntz–Krieger
isometries on the word-Fock space with Toeplitz defect ΣSS* = 1−P_seed EXACT; the external
uniqueness theorems (Enomoto–Fujii–Watatani; aHLRS geography) attach to our own object.
**THE TEMPERATURE DICTIONARY (chronic trap — four numbers, never conflate):**
h_top = ln 2 (per-tick topological entropy; THE CRITICAL CK POINT = the Landauer lock) ·
β_gas = −ln α₁ (the power-1 gas) · β_eff = 2 ln(u_c/α₁) = 5.1011473686 (the Born-2/ladder-algebra
temperature, G5a) · 2β_gas = 6.4874 (the run's own per-path diagonal KMS temperature — ABOVE
critical, where seed-anchored states live). Per-path vs per-shell (degeneracy-weighted) readings
differ by exactly h_top (β′ = β_natural − h_top, offset −ln 6, exact — FOCK-0e m5).
**[ADJUDICATED 2026-07-12, Push-3 W3b — the dictionary is now a LAW, not an open question:**
every live load-bearing usage has a construction-forced reading (census with receipts:
internal research notes); RULE: every NEW temperature/rate usage
states its forced reading at freeze time. β′ numerically equals a refuted I2-era candidate by
two unrelated arguments — never identify them without a derivation.]
**TWO H's:** H_sub = 1/(N·t_P) (counting spine) ≠ H_metric = ȧ/a. The observer/substrate rate gap
is 16/15, priced PER POWER OF THE RATE (H¹→16/15, Λ = H²→(16/15)²); dimensionless quantities'
power assignments are UNDERIVED (the A_s frame issue) — the clock map owns this.
**THE CALIBRATION FENCE:** exactly ONE dimensional input exists — measured G_F tethers N_hub.
v/G_F and 19 downstream rows are the tether's own curve, structurally excluded from epoch
prediction. N_hub is a calibration, not an observable. e_bit = 1; M_Pl = 8/√π natural.
Pointers: M0-2R files (`proofs/foundations/M0_2R_*`), `I2_matsumoto_confront_2026-07-10.py`,
`I2b_matsumoto_completion_2026-07-10.py`, the S1d section of `the_run.py`.

## §5 THE MASTER CHIRALITY LENS (the single most load-bearing intuition)
**Bit-EVEN = democratic = BLIND. Chirality lives in the iJ, never in the spectrum.** The forced
chiral seed ⟨0|U_π²|0⟩ = i/2 (Re = 0 forced; sign flips with the bit); J = the A4 3-irrep.
Proven blindness now covers: every even functional (O0) · all linear intertwiners (M-1b exact
perpendicularity) · all R-parity-definite attachment functionals (BRIDGE-LOCK, 3 lemmas) · ALL
seed-anchored state-level two-point data to all orders (BRIDGE-T symmetric-compression lemma:
Uoᵀ(I−uB)⁻¹Uo is exactly symmetric AND reversal-invariant) · spatial lattice chirality
(BRIDGE-GEOM enantiomer-blindness theorem). **[2026-07-11, three MORE nulls — the FOCK arc]:**
· generator-equivariant dart↔field maps are IDENTICALLY ZERO (projectivity-mismatch lemma:
honest-A4 dart rep vs 2T-projective field algebra; also kills any 2T-lift of the dart carrier) ·
a single antiunitary(J)-pin is GENERIC (always leaves exactly half the space — zero framework
content alone; per-sector J+parity pins leave 16 dims, quark-pair block exactly 0 at shell 1) ·
**THE CLOCK-INCOMMENSURABILITY OBSTRUCTION (W2, theorem + verified): NO shell-uniform
linear map intertwines the two derived clocks at ANY single rate λ — the history clock is linear
in word-length (c_n; c₂=2c₁ exact) while a region's field clock has ONE energy magnitude (C_A
eigenvalue exactly ½ forced). THE EMBER: per-shell maps EXIST at solved λ* = c₁/ε = 2.463 —
the failure is only ONE-RATE-FOR-ALL-SHELLS.** The load-bearing bit is **TEMPORAL** (TID2-D) and
**per-sector**. Consequence (sharpened 2026-07-11): any dart↔field identification must be
phase-bearing, per-sector, temporal, AND **level-graded with a shell-dependent clock rate —
LINEAR-IN-SHELL-UNIFORM is now a proven dead class.** Design any new probe against this list FIRST.

## §6 SPECIES, SECTORS, CHARGE
Color = the Cl(6) Fock construction, NOT M₃(ℂ). The internal Cl(6) is the KO-dimension-6 exotic
presentation ≡ the NCG Standard Model's anatomy. Sectors = A4 (2T double cover) irreps with species
map {ν:1, d:3, u:3, e:1}; twisted (Klein) locality is FORCED (odd sectors anticommute).
**Theorems (MS-1a, computed):** fermion parity is the UNIQUE nontrivial Z₂ grading of the fusion
ring; additive ℤ-charges are identically zero (⟹ no exact baryon-number protection — consistent
with η_B's Sakharov requirement; stability must be a suppression statement); N̂ (hence 3Q = N̂) does
NOT descend to the fusion ring. Q = N̂/3 is the derived charge. The DR/sector frame is conditional
on thermodynamic-limit twisted Haag duality (ML-2b) — carry the qualifier.
Pointers: `the_net.py` (§ gauge_sector_category + §7 accretions), `derivation_topdown/adapters/`
(the G-suite contracts), `MS1a_fusion_grading_2026-07-10.py`.

## §7 THE INTERACTION ANATOMY (hard-won this week; do not regress it)
- **The vertex** = the MDL contact coupling −κ·I(A;B): Γ⁵-EVEN (passes the chiral filter),
  field-content-free, proven CONTACT (range 0), deep regime (U = 3 ≫ U_c ≈ 0.26).
- **The connection** = W_INT (Γ⁵-ODD, dart-off-diagonal, kinetic/transport). Its compliant
  (R-odd/signed) decoration is verified: the forced chiral asymmetry SURVIVES re-decoration
  (magnitude was convention-dependent; the orbit ambiguity = an overall sign).
- **At composite scale the certified interaction functional is ΔS = (edge-overlap information) −
  (vertex-branching cost)** — NOT bare co-information (the identity: rung 3 = 5−2, rung 13 = 15−2).
  The ladder {2-body: 1,3 · 3-body: 1,2,3,4,6,13} is certified ONLY on distinct-cycle
  configurations; the coincidence value is UNDEFINED (the open domain question; exchange
  statistics/fermion parity is the candidate forcing).
- **E_bind = −κ·ΔS is the topological FACTOR; T₀(μ_eff) is the kinetic term** (derived: T₀⁽²⁾ =
  0.3311 = 11% of U). The contact class has μ-exponent ≈ +0.12 (deep-contact), NOT linear-μ ⟹
  Coulomb-class binding (hydrogen/positronium's 10.9-ppm reduced-mass law) belongs to the
  CONNECTION sector.
- **The finite-k theorems:** cover gauge-triviality + holonomy triviality (192/192) prove the
  vacuum/k=0 gauge response is exactly zero ⟹ the photon/1/r kernel can ONLY be the finite-k
  k²-coefficient (the Maxwell term) — the named build (Kubo ∂²K/∂B², window-dilution-free).
Pointers: `I0a_reconciliation_2026-07-10.py`, internal research notes,
`IV4_T0_class_2026-07-10.py`, internal research notes.

## §8 THE WELD — ARC RUN AND ANSWERED (Push 2, four stations, CLOSED 2026-07-12)
THE WELD (the history→field identification, third-time-independently-named 2026-07-11) was run
to ground in Push 2 (A1 + A2/A2b/A2c/A2d; every verdict verified or waypoint-verified).
**ANSWER: THE WELD EXISTS BUT IS NOT FORCED.** A1 first PROVED the history clock exactly linear
(c_n = n·c₁, algebraic identity; c₁ = β_natural = −2 ln α₁). Then, in the minimal justified
class (grading + level-1 A4-equivariance — every other pin REFUTED by theorem), the weld is a
**ℂP² family over the 3-dim multiplicity space of the 3-irrep in shell-1**: it covers F levels
1⊕2⊕3 (vacuum unreachable), the conjugate weld Φ̃ = J_F∘Φ∘J_hist sits EXACTLY at level 2, and
the image couples to every region-clock eigenspace (±ε included). Verified functor facts: the
fermionic tower cuts at shell 3 (P1); exclusion lives in the IMAGE (P2 — first repeated dart at
shell 4; the 864/864 lemma is its shadow).
**THE FOUR DICTIONARY CORRECTIONS (each an obstruction theorem):** J_F = THE ANTIPARTICLE
PAIRING (levels 0↔3/1↔2 = ν↔e/d↔u), grading-REVERSING ⟹ no self-J-pinned graded weld (AF-3 +
L0a: all level-additive self-J welds die beyond shell 1) · fermion parity = word-LENGTH parity
AUTOMATICALLY (the R/F_bit reversal dictionary REFUTED, B3) · region clocks can NEVER pin the
tower (1-dim positive eigenspace rank-starves; N̂ at rate c₁ = THE field clock — explains every
shell-1-only ember) (L0b) · the sector-pair-block pin = species-input-in-disguise,
Schur/character-orthogonality-refuted (C3).
**THE ONE OPEN: THE MULTIPLICITY SELECTOR** — what selects the weld's direction in the 3-dim
multiplicity space. WHY IT EVADED EVERYTHING: Schur — the multiplicity space is exactly what
every equivariant/modular (democratic) structure cannot see; the selector must come from
DYNAMICS (the vertex on the family) or LOCALITY (region-by-region modular consistency = the
net). **FOCK-2 (numeric M_Z/ppm confrontation) is GATED on the selector** — confronting an
unselected direction = goal-seek, forbidden. **STOPPING RULE (held): no further weld stations
without a theorem-grade selector source.** The hunt rides Push 3 (the modular-alignment locus
on the family, a cheap READ on machinery Push 3 builds anyway) and the vertex lane (Push 5).
Generation-resemblance of the 3-space = OUTLOOK REGISTER ONLY.
Work order: internal research notes; full arc: the A2/A2b/A2c/A2d
pre-regs + working notes; register: `milestones_2026-07-10.md` II.2 block; memory:
weld-arc-push2-closed-2026-07-12.

## §9 THE TRAP LIST (each has burned a session at least once)
α₁ ≠ α_EM · the four temperatures (§4 dictionary) · two H's · srs-z ≠ enantiomer · rung =
info − branch cost (bare co-information over-counts) · the ladder's domain (distinct cycles only) ·
A_s's 16/15 power is UNDERIVED (never pick the closest-landing power) · per-path vs per-shell ·
the P point is where physics lives, but the O(2) map family dies there (per-sector objects) ·
assembly/implementation pass agents FABRICATE PROVENANCE — verify every citation by opening the file ·
implementation passes UNDER-IMPLEMENT frozen plural constraints (two verifier catches in one day:
generator-class instead of the pinned class; ONE global pair where the freeze said per-sector) —
checkers must re-derive the constraint COUNT, not just rerun · untracked background monitors DIE
SILENTLY under load (agents waited hours on processes that no longer existed) — use tracked
tasks, and ps-check any agent "still waiting" past its expected runtime · linear-in-shell-uniform
dart↔field maps are a PROVEN dead class (§5 W2) — do not re-test linear welds ·
piping verify through `tail` masks the exit code — always capture RESULTS explicitly · the ledger
prose drifts; locks + manifest + engine are the value truth · auto-sync cron commits every ~30 min
(expect it; use explicit commits for records) · verify under load false-fails (rerun uncontended) ·
INHERITED PINS OUTLIVE THEIR JUSTIFICATIONS — audit every pin's PROVENANCE at freeze time (the
pair-block pin was the 4th dictionary error; it survived two stations on inheritance alone and
encoded species as input) · `_field_algebra_a4_rep`/`spin_lift` are QUARANTINED (phase ambiguity;
no adjudicated result routes through them; do not reuse) · consistency tests can be VACUOUS
(Λⁿ of an equivariant map is automatically equivariant — consistency ≠ forcing; write the
honesty clause into every freeze) · SERIALIZE heavy compute (L9: ONE CPU-bound station run at a
time, OMP_NUM_THREADS=4, full suites alone on a quiet box — 3 concurrent runs cost >10× once).

## §10 ORDERED READING (after this file)
1. internal research notes — THE CURRENT WORK ORDER (the governing arc;
   a model DRIVES; DONE = enumerate-and-price the non-derived content + ≥1 ahead-of-data prediction +
   one-command reproduction; THREE LANES: SHIP → ARMOR → BUILD-the-cascade). Pushes 1+2+3 CLOSED and
   the GEN-IDENT arc CLOSED both routes (2026-07-15/16); Lane 1 step 1 = the repo-hygiene/public-push
   handoff internal research notes. (Historical work orders:
   `handoff_PUSH3_region_2026-07-12.md` — Push 3, the selector arc, verify.py 105→110 green.)
1b. internal research notes — THE INTENT LAYER (user-ordered
   2026-07-12): the lesson ledger, the architect's opinion per frontier, the push queue. Every
   fresh push handoff derives FROM its §3; the architect updates it at every push close and every
   opinion-changing adjudication. Status → register wins; execution → handoff wins; intent → it wins.
2. internal research notes — the parameter-complete register + critical path (STATUS).
3. The weld-arc pre-regs (A2/A2b/A2c/A2d, internal research notes*_prereg_2026-07-12.md`) + their
   returns/checks — the four obstruction theorems, the dictionary corrections, the ℂP² family,
   THE MULTIPLICITY SELECTOR. (The FOCK-0 pre-reg + amendments = the parent arc, on demand.)
4. `docs/incomplete_equations_todo.md` — top section + the latest bullets (the working queue).
5. internal research notes + internal research notes
   — the selection math + the paper pipeline/voice contract (Movement 1 SHIPPED to the user).
6. `derivation_topdown/bridge/the_run.py` header + `derivation_topdown/state/the_net.py` header
   (the net now carries §7b I2b algebra · §8 grading/J_σ · §8b Tomita · §8c full-pin · §8d flow).
7. On demand: internal research notes for full reports; `docs/audits/mdl_ledger.md`.
